@model List<PortfolioDataDto>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Areas/Admin/Views/AdminLayout/Index.cshtml";
}

<div class="col-md-12">
    <div class="chart-box">
        <h4>PortfolioData Table</h4>
        <div class="bs-example" data-example-id="bordered-table">
            <table class="table table-bordered">
                <thead>
                    <tr>
                        <th>Name</th>
                        <th>Url</th>
                        <th>ImagePath</th>
                        <th>Delete</th>
                        <th>Update</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>@item.Name</td>
                            <td>@item.Url</td>
                            <td>@item.ImagePath</td>
                            <td><a href="/Admin/PortfolioData/DeletePortfolioData/@item.Id" class="btn btn-danger">Delete</a></td>
                            <td><a href="/Admin/PortfolioData/UpdatePortfolioData/@item.Id" class="btn btn-success">Update</a></td>
                        </tr>
                    }
                </tbody>
            </table>
            <a href="/Admin/PortfolioData/CreatePortfolioData/" class="btn btn-primary">Create PortfolioData</a>
        </div>
    </div>
</div>
