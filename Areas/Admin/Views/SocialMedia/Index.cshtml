@model List<SocialMediaDto>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Areas/Admin/Views/AdminLayout/Index.cshtml";
}

<div class="col-md-12">
    <div class="chart-box">
        <h4>SocialMedia Table</h4>
        <div class="bs-example" data-example-id="bordered-table">
            <table class="table table-bordered">
                <thead>
                    <tr>
                        <th>Title</th>
                        <th>Url</th>
                        <th>Icon</th>
                        <th>Delete</th>
                        <th>Update</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>@item.Title</td>
                            <td>@item.Url</td>
                            <td>@item.Icon</td>
                            <td><a href="/Admin/SocialMedia/DeleteSocialMedia/@item.Id" class="btn btn-danger">Delete</a></td>
                            <td><a href="/Admin/SocialMedia/UpdateSocialMedia/@item.Id" class="btn btn-success">Update</a></td>
                        </tr>
                    }
                </tbody>
            </table>
            <a href="/Admin/SocialMedia/CreateSocialMedia/" class="btn btn-primary">Create SocialMedia</a>
        </div>
    </div>
</div>
